# Revisions to skip in `git blame`, so a whole-repo reformat does not make
# every line look like it was last touched by the formatting commit.
#
# Configure once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Formatting-only commits (no behaviour change) go here, newest last.

# style: apply Prettier across the codebase (2026-08-08)
# Formatting only — lint, typecheck, both builds, and all 165 tests passed
# identically before and after.
2199e4b0507baf1882d898ba28ca9a3f2d357df5
