{
  "name": "salmoncow",
  "version": "1.0.0",
  "description": "Salmoncow - A modern web application with Firebase authentication and Google sign-in",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0 <25.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build:assets": "node scripts/build-assets.js",
    "build:env": "node scripts/inject-env.js",
    "build": "npm run build:assets && npm run build:env",
    "dev": "npm run build && npx http-server public -p 3000",
    "clean": "rm -rf public/js public/assets",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy preview",
    "firebase:open": "firebase open hosting:site"
  }
}
